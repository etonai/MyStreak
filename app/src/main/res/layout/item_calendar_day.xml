<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="52dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Oval background — color set programmatically via setBackgroundTintList -->
    <View
        android:id="@+id/view_day_bg"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_calendar_day" />

    <!-- Day number text centered over the oval -->
    <TextView
        android:id="@+id/tv_day_number"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:gravity="center"
        android:textAppearance="?attr/textAppearanceBody2"
        android:textStyle="bold" />

    <!-- Today ring overlay — visible only for today -->
    <View
        android:id="@+id/view_today_ring"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_calendar_today_ring"
        android:visibility="gone" />

</FrameLayout>
